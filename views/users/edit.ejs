<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
</head>
<body>
    <h1>Edit User</h1>
    <form action="/users/<%= user._id %>?_method=PUT" method="post">
        <div>
            <label for="name">Full Name</label>
            <input type="text" id="name" name="user[fullName]" value="<%= user.fullName %>"> 
        </div>
        <div>
            <label for="email">Email Address</label>
            <input type="email" id="email" name="user[email]" value="<%= user.email %>">
        </div>
        <div>
            <label for="hobbies">Favourite Hobby</label>
            <input type="text" id="hobbies" name="user[hobbies]" value="<%= user.hobbies[0] %>"> <!-- Use 'hobbies' -->
        </div>
        <div>
            <label for="location">Location</label>
            <input type="text" id="location" name="user[location]" value="<%= user.location %>">
        </div>
        <div>
            <label for="password">Password</label>
            <input type="password" id="password" name="user[password]" value="<%= user.location %>">
        </div>
        <button>Update My Details!!</button>
    </form>
    <footer>
        <a href="/users/<%= user._id %>">Back To User!!</a>
    </footer>
</body>
</html>
